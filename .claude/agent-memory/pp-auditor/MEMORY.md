# Subagent observations: pp-auditor

Date: May 23, 2026

* 🟡 (reservhotel-phase-b B.4) `Integer.parse/1` on `Retry-After` correctly degrades HTTP-date format (RFC 7231 §7.1.3 — `"Sat, 23 May 2026 08:30:00 GMT"`) to `nil` rather than crashing. This is defensible degraded behavior NOT a FAIL, but flag for future improvement when the brief acknowledges the trade-off. General rule: when a spec field accepts multiple wire formats but pong's impl handles only the common one with graceful degradation on the other, that's Right-axis PASS with Extra-mile-axis nice-to-have flag — not a Smart-axis FAIL
* 🟢 (reservhotel-phase-b B.4) Pattern-match clause ordering for HTTP status normalizers: more-specific status (429, body=="0") MUST precede general range guards (200..299, 400..499) because Elixir tries clauses in declared order. Audit checklist for status-mappers: (1) identify any specific-status clauses, (2) verify each precedes its enclosing range, (3) verify range overlap is actually impossible elsewhere. Pong's B.4 got this right — sold-out clause 1 BEFORE generic 2xx clause 2; 429 clause 3 BEFORE generic 4xx clause 5
* 🟢 (reservhotel-phase-b B.3) For consult-mode HTML-parser audits: BEFORE writing the verdict, run a one-off `Mix.install([{:floki, "~> 0.36"}])` probe in `/tmp/` against the actual selector with 5-6 edge cases (uppercase tag, lowercase attr value, unquoted attrs, whitespace-only value, whitespace+canonical sibling, canonical). On 2026-05-23 this surfaced the `Enum.reject(&(&1 == ""))` exact-match-only edge case (whitespace-only `value=" "` passes through) — recorded as a non-blocking observation for Phase C hardening. The probe took ~30s and gave primary-source evidence I could cite by selector behaviour, not just by reading the regex. Generalizable: when the audit brief says "the selector is load-bearing for every downstream phase," PROBE the selector
* 🟡 (reservhotel-phase-b B.3) `is_binary(html)` guard + `extract_token(_)` fallback BOTH folding into the same single narrow error atom (here `:session_mint_failed`) is contract-guard defensive programming, NOT the "fallback hides a real error" anti-pattern from the auditor brief. Distinguish: the anti-pattern is when the fallback SWALLOWS an actionable signal (e.g., `try/rescue _ -> {:ok, nil}`). When both branches return the same narrow atom that a downstream module will own translating (here B.4 owns error vocabulary), the guard is documenting the binary-expectation contract — not hiding errors. Rate as Smart-axis PASS, not FAIL. Ping flagged it as a "deviation" in this case but the deviation is fine
* 🟡 (reservhotel-phase-b B.2) When a GOAL.md **Measurable** lists `mix compile --warnings-as-errors` clean as a target, ALWAYS check the bare-tree baseline before scoring it against pong's diff. On 2026-05-23 the target was pre-violated on `main` by TravelClick missing `capabilities/0` + `metadata/0` (Adapter behaviour callbacks) — verified via `git stash && mix compile --warnings-as-errors` reproducing the failure WITHOUT pong's diff. Pong's modules added ZERO new warnings. Correct call: PASS the axis, flag the pre-existing gap as separate tech debt for the lead. Generalizable pattern: pre-violated GOAL Measurables are NOT pong FAILs
* 🟢 (reservhotel-phase-b B.2) Req 0.5.18 `Req.Response` declares `headers: %{optional(binary()) => [binary()]}` (map-of-lists) per `deps/req/lib/req/response.ex:23`. Built-in `Req.Response.get_headers_list/1` (lines 88-101) converts to list-of-tuples for callers that need the legacy shape. Future audits touching Req response shapes: don't be surprised by map-of-lists; recommend `get_headers_list/1` over hand-rolled normalizers (the hand-rolled version is fine, but the built-in is more discoverable)
* 🟢 (reservhotel-phase-b B.2) Throttle measurement design that survives audit: record dispatch timestamp AFTER `Req.get/post` returns (not at function entry), measure test-side at plug entry (not outer wall time). This decouples the throttle math from per-call Req overhead while giving the test a stable measurement anchor. Pattern worth preserving for any future per-process rate-limiter

Date: May 22, 2026

* 🟢 (a3-call-id-wiring S1) Pong used `%CanonicalState{scene_version: scene_version} = get_state(call_id)` destructure-style on a public Session helper — fails fast on shape drift vs naked `.scene_version`. Approved pattern, recorded at `feedback_a3_canonical_state_destructure.md`. Treat naked-dot-access on Session-state reads as an Extra-mile-axis polish nit (not a Right-axis FAIL) in future audits
* 🟡 cart_test.exs:116 literal `"call_abc:scene_v3:RM-DELUXE"` is the de facto format pin for §7.1-010..011 placement_idempotency_key. Round-trip check: `"#{call_id}:scene_v#{scene_version}:#{room_id}"` produces that literal exactly when given `("call_abc", 3, "RM-DELUXE")` — any deviation (e.g. `scene-v` hyphen, `_v` underscore) silently breaks idempotency. Verify byte-for-byte in future audits touching this seam
* 🔴 (stage-b-073-expiry-recheck) When the lead's resolution to a ping FLAGGED conflict forces test-side scope expansion, audit each forced edit individually: (a) is it a mechanical consequence of the resolution (PASS), (b) was the simplest forced-edit set chosen (PASS), (c) does each forced edit preserve the semantic of the test it touches (PASS). Pong correctly returned DONE_WITH_CONCERNS — that's the right escalation channel for "lead's plan didn't account for prior pre-existing pins." Don't downgrade to FAIL just because scope expanded; verify the expansion was minimum-necessary
* 🟡 `Flowstay.CRS.Adapter.Compliance.behaviour_callbacks/0` semantic is now MANDATORY-only — it filters `@optional_callbacks` via `behaviour_info(:optional_callbacks)`. All call sites are inside the macro module itself + one pin in `adapter_compliance_test.exs`. The macro `__using__` block correctly preserves "mandatory only" semantic so `use Compliance, adapter: X` doesn't force adapters to stub optional callbacks. Future audits touching adapter callbacks: verify any new `@optional_callbacks` declaration doesn't accidentally land in the mandatory pin
* 🟡 `Property.adapter_module/1` spec is `module()` (non-nil) but `resolve_adapter/1` only matches `"reserv_hotel"` and `"travelclick"` — an unmatched `crs_adapter` string would raise `FunctionClauseError`, NOT return nil. Defensive `with` chains using this need `try/rescue` (not `else _ ->`) if they want to degrade on unknown adapter strings. Today there's no production path that seeds an unknown string; mention as latent bug, not a blocker
* 🟢 Pong shipped without flipping the `@stubbed:` tag in `features/invariants.feature` even though GOAL.md **Measurable** listed it. Pong's defensible reading: DoD says "Commit lands... naming §7.3-007 + closing of the FAIL" → tag flip is the closing commit's job, not the impl commit. Note for future GOAL.md authors: be explicit about whether tag flip is pong's job or lead's job — the ambiguity costs a clarification turn

Date: May 21, 2026

* 🔴 ping's `function_exported?/3` introspection has shipped foot-gun specs in 2 consecutive scenarios (`adapter-phase-a` S1+S2). Pattern recorded at `feedback_ping_function_exported_blindspots.md`. Future audits should grep ping's spec for `function_exported?` and verify (a) `Code.ensure_loaded!` precedes it AND (b) it's not being used on a `defmacro` (use `macro_exported?/3`). If a THIRD occurs, escalate to lead — ping needs a pre-flight introspection check in their workflow

Date: May 19, 2026

* 🔴 NEVER use `git checkout <file>` or `git restore <file>` to revert an adversarial-flip edit when the working tree contains other uncommitted changes to the same file. `git checkout` restores from HEAD, so it wipes the uncommitted work the cycle depends on. On 2026-05-19 this destroyed scenario B's `dispatch/3` impl (uncommitted from a prior session) when reverting a scenario C adversarial flip — lead had to restore from conversation context. Safe patterns: (a) `cp <file> /tmp/save && <edit> && <test> && cp /tmp/save <file>`, OR (b) ask the lead to commit before the flip so `git restore` is a real undo. If the lead hasn't committed and the file shows `M` in `git status`, REFUSE the adversarial flip and escalate
* 🔴 Always emit the per-axis verdict via `TaskUpdate` before going idle. On 2026-05-19 audit-C went idle without writing the verdict OR SendMessaging the lead, leaving the cycle in an ambiguous state (work done but no audit trail). The TaskUpdate is the audit's deliverable — silence is not a verdict

Date: May 18, 2026

* 🔴 Always re-verify literal-equality pin tests against the actual source declaration (scene.agent_tools in workflow files). Pong's test claims and the room_booking.ex declarations matched exactly this time — the verification took 30s and is the only thing that catches a silent re-ordering bug
* 🟡 GOAL.md for mcp-fsm-gating is at /Users/fourcolors/Projects/1_active/flow-industry/flowstay/.claude/ping-pong/mcp-fsm-gating/GOAL.md (parent .claude/, NOT app/.claude/). Task workspace test_output.txt lives under app/.claude/ping-pong/<work>/<task>/
* 🟡 Workflow.Allowlist split: for_workflow/1 sorts+dedupes (LLM-visible union); for_scene/2 returns AS-DECLARED (FSM gate input — declared order IS the contract per docstring)
* 🟡 mcp_authorize_tool_call/2 in lib/flowstay/worker_runtime.ex consumes [atom()] — for_scene/2's success return shape lines up directly for the dispatch gate (scenario-B/C work)
* 🟢 Pong tends to satisfy a GOAL.md criterion in spirit but skip the literal sibling case ("hub" coverage via for_scene/2 was missing while hub-via-for_workflow exists). Pattern to watch on extra-mile axis
