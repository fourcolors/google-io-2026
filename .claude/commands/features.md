---
description: Run / extract / update / status the FlowStay feature catalog. Loads the features skill.
argument-hint: "[run|extract|update|status] [tag-filter] [flags]"
---

Invoke the `features` skill with the provided arguments: $ARGUMENTS

The skill's `SKILL.md` documents the full procedure across all modes.
